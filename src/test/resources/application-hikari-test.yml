# HikariCP 测试配置
minitb:
  storage:
    type: sqlite
    sqlite:
      path: data/test-hikari.db
      use-hikari: true  # ⭐ 启用 HikariCP

spring:
  jpa:
    hibernate:
      ddl-auto: none

logging:
  level:
    com.zaxxer.hikari: DEBUG
    com.minitb: DEBUG

